apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "describer.fullname" . }}-test-connection"
  labels:
    {{- include "describer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "describer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
