apiVersion: v1
kind: Service
metadata:
  name: {{ include "describer.fullname" . }}
  labels:
    {{- include "describer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "describer.selectorLabels" . | nindent 4 }}
